# API Gateway base URL printed by infra/api.sh. Server-side only: it is never
# sent to the browser, which is why the API needs no CORS configuration.
DATATRACE_API_URL=https://xxxxxxxxxx.execute-api.us-east-2.amazonaws.com
